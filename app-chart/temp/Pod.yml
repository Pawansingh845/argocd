apiVersion: v1
kind : Pod 
metadata :
  name: {{.name}}
  # labels:
  #   app : nginx
  #   env : dev
  #   tier : frontend

spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80

    
    - name: nginx1
      image: nginx
      ports:
        - containerPort: 81

---
apiVersion: v1
kind : Pod 
metadata :
  name: anil1
  # labels:
  #   app : nginx
  #   env : dev
  #   tier : frontend

spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80
