apiVersion: v1
kind : Pod 
metadata :
  name: anil
  # labels:
  #   app : nginx
  #   env : dev
  #   tier : frontend

spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80

    
    - name: nginx1
      image: nginx
      ports:
        - containerPort: 81