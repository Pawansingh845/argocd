apiVersion: v1
kind : Pod 
metadata :
  name: anil
  # labels:
  #   app : nginx
  #   env : dev
  #   tier : frontend

spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80

---
apiVersion: v1
kind : Pod 
metadata :
  name: anil1
  # labels:
  #   app : nginx
  #   env : dev
  #   tier : frontend

spec:
  containers:
    - name: nginx
      image: nginx
      ports:
        - containerPort: 80
